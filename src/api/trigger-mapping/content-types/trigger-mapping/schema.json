{
  "kind": "collectionType",
  "collectionName": "trigger_mappings",
  "info": {
    "singularName": "trigger-mapping",
    "pluralName": "trigger-mappings",
    "displayName": "Trigger Mapping"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "eventKey": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "USER_WELCOME",
        "OTP_VERIFICATION",
        "PASSWORD_RESET_OTP",
        "FACILITY_VERIFIED",
        "DOCUMENT_REJECTED",
        "INVITE_RESIDENTIAL",
        "INVITE_COMMERCIAL",
        "INVITE_OPERATOR",
        "INVITE_PARTNER",
        "AGREEMENT_RESIGN",
        "ADMIN_REPLY",
        "ADMIN_REMINDER",
        "UTILITY_PROVIDER_REQUEST",
        "FINANCIAL_TYPE_REQUEST",
        "UTILITY_AUTH_SUCCESS",
        "UTILITY_AUTH_EXPIRED"
      ]
    },
    "template": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::template.template"
    },
    "fallbackTemplate": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::template.template"
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "audience": {
      "type": "enumeration",
      "enum": [
        "Customer",
        "Partner",
        "SalesAgent"
      ]
    }
  }
}
